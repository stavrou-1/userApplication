import { SelectedColorDirective } from './selected-color.directive';

describe('SelectedColorDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectedColorDirective();
    expect(directive).toBeTruthy();
  });
});
